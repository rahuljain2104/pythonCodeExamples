from utils import MLUtilities as util

# read data
params = util.get_config_file()
df = params['mandatory']['train_csv_file_path']

# analyze data
df['']

# process data

# create training and test sets

# perform classification and predict the test set
